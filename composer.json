{
    "require": {
        "geoip2/geoip2": "~2.0",
        "symfony/console": "^6.0"
    },
    "require-dev": {
        "symfony/var-dumper": "^6.0"
    },
    "scripts": {
        "post-install-cmd": [
            "mkdir -p ./logs",
            "tar -xf GeoLite2-City.mmdb.tar.gz"
        ]
    }
}
